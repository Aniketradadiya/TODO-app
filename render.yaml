services:
  - type: web
    name: focusflow-todo-app
    env: node
    plan: free
    buildCommand: npm install --prefix backend
    startCommand: npm start
    envVars:
      - key: PORT
        value: 10000
      - key: DATABASE_URL
        sync: false # Set this in Render Dashboard or connection settings
